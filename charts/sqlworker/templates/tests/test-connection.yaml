apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqlworker.fullname" . }}-test-connection"
  labels:
    {{- include "sqlworker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqlworker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
